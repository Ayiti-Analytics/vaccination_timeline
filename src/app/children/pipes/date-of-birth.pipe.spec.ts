import { DateOfBirthPipe } from './date-of-birth.pipe';

describe('DateOfBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOfBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
